export type UserInfo = {
    firstName: string
    surName: string,
    city: string,
    municipality: string,
    zipCode: number,
    adress: string,
    phoneNr: number | null,
    co: string | null,
    id: string
}